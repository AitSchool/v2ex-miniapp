<import src="/template/topics/topics.wxml"/>

<view class="page-container">
  <!-- <navigator class="topics-item"  url="/pages/show/show">
    <image class="topics-avatar" src=""></image>
    <view class="topics-content">
      <view class="topics-username">姓名</view>
      <view class='topics-title'>主题标题</view>
    </view>
  </navigator> -->

  <!-- <navigator class="topics-item" wx:key="unique" wx:for="{{topics}}" url="/pages/show/show?id={{item.id}}">
    <image class="topics-avatar" src="{{item.member.avatar_normal}}"></image>
    <view class="topics-content">
      <view class="topics-username">{{item.member.username}}</view>
      <view class='topics-title'>{{item.title}}</view>
    </view>
  </navigator> -->

  <template is="topics" data="{{ topics }}"/>
</view>